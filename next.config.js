/** @type {import('next').NextConfig} */
const nextConfig = {
  // Set exportTrailingSlash to true to add trailing slashes to URLs (optional)
  exportTrailingSlash: true,

  // No static export here, this is the default SSR setup
  // You can configure other Next.js settings as needed
};

module.exports = nextConfig;
